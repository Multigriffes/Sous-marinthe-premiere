{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run current Python file as module",
      "type": "debugpy",
      "request": "launch",
      "pythonArgs": ["-i"],
      "module": "${relativeFileDirname}.${fileBasenameNoExtension}",
      "cwd": "${workspaceFolder}"
    }
  ]
}